# Five Test Example Demo

These files are generated from the already completed CRNN/CNNT test predictions. The `.krn` files are the real symbolic model outputs. The `.wav` files are simple sine-wave renderings for presentation playback only.

- `crnn/` and `cnnt/`: input images, predictions, ground truth, and audio.
- `demo_manifest.csv`: one row per model/example.
